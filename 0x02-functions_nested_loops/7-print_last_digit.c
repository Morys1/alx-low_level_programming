#include "main.h"

/**
 * print_last_digit - Function to print the last digit a number.
 * @n: the input number
 * Return: x if x is greater than zero and 0 if x is zero -1 otherwise
 */
int print_last_digit(int n)
{
	
	return (n % 10);
}
